VITE_API_BASE_URL=http://localhost:8080/api/v1
# Toggle mock API mode (true/false). When true, endpoints return mock data without network calls.
VITE_USE_MOCK_API=true
# Upload mode for resume files: multipart (default) or s3.
VITE_UPLOAD_MODE=s3
